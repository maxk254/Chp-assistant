import React from 'react';

const Supervisor = ({ patients = [] }) => {
  return (
    <div className="max-w-7xl mx-auto py-8 px-8">
      {/* Blank Supervisor Dashboard */}
    </div>
  );
};

export default Supervisor;

